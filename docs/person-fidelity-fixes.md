# 人物保真与交付核验修复

当前安装包为 `photo-dialogue-2a6156c.zip`，运行提交 `2a6156c5369a30c8ad28ec1e79533c4aa286671c`；SHA-256 为 `719007587cc894b324ea3f7c2407c0e9a95a7cd2b39db29c3fe4f7eeed2ca9bd`。可复现构建、独立安装/自检/卸载、34 项本机测试、类型与仓库检查通过。两项定向复审没有剩余具体问题。

## 修复与证据范围

历史密集成图把文字栏挤进摄影区域，导致非等比人物压缩、裁肢或补出画外头发；旧核验只看“人物和动作大体相同”，曾误交付一张。现在先核对摄影比例及边缘，再逐人比较原图可见结构、表情和动作。优先用真实留白放短旁白；不得静默缩短已确认原话。原有遮挡不要求补全，光色、细微颗粒与背景景深变化不单独视为人物漂移；原图不支持的新结构仍拒收。保留用户明确要求的背景隐私与画幅处理。

修改时以选定的已接受旧版清洁副本为唯一编辑底图，原图用于本地人物核验；不混用最新版本。1–2 像素栅格、抗锯齿和纹理差异不单独等于失败，但明显非请求排版与人物变化仍拒绝。像素差比例不能代替视觉判断，也不能据此承诺逐像素编辑。

18 次影像矩阵、行为与修改使用固定 `ad0104e`，包 SHA-256 为 `67340672f4377f759e053d22e65bf05bd844810f64580953eaa23fba524130c4`。最后的 `2a6156c` 仅补充本地预览和下载必须使用实际存在的绝对路径，图像生成及核验指令未变。两个曾输出相对链接的作品在新包干净宿主中重显成功，零生成、文件哈希不变。没有宣称新链接包重新跑了 18 次影像生成；依验收合同，新包的完整矩阵仍需重测。这里引用前一固定包仅用于诊断，不转移发布成绩。

## 完整矩阵结果

固定九张输入各两轮、18 次真实内置图像调用全部完成，未更换难样本或跨批次选优：15 次通过独立底线核验、3 次拒收，未观察到人物误交付。15 张均检查完整画面和 390 CSS px 预览，人工五项分数全部留空。两个相对链接缺陷另行计数，已用最终补丁回归。

- 近景实拍 real-01 两轮文字在手机宽度过小，均未接受为成品。
- 低照多人实拍 real-03 第一轮补出不能从原图确认的面部结构，未接受为成品；第二轮通过底线。
- 六人合成图两轮保留完整横幅与人物，不再挤压摄影区域。

当前仍低于至少 16/18 且每张输入至少一次成功的发布门槛。人工评分不能抵消三次失败；#26、#17 不能标为完成。低照人物生成和小字控制仍有限制，未观察误交付也不是保真保证。

## 修改、行为和工程核验

首次三项修改为 2/3：只改文案、减轻氛围通过；“整体上移约 80px 并保留字距行距”的严格测试因排版改变拒收。随后单独按产品自然语言要求“从 v001 把原句移到顶部”成功生成 v003，未混入 v002 新文案，原版本哈希保持。三条产品路径都有成功证据，共四次真实调用；不把首次失败改写成成功，也不承诺精确像素位移。

15 类行为用例核验通过，包括授权、原话、输入边界、未成年人、失败保护、重试/停止、恢复及导出。17 次受控服务替身调用与真实生成分开记录；七个边界会话零生成，三个补充行为会话各一次真实生成。长引语固定像素尺寸不符时拒收，未静默缩短原话。文字、无障碍和明确标注“重建、非历史原文”的提示词导出已检查。

五个只读诊断回归为三张历史负例正确拒绝、两张正常对照正确接受，零生成。这只证明五个已知用例。34 项测试在最终包本机 CPython 3.13.13 重跑通过；macOS/Linux arm64 × CPython 3.11.15、3.12.13、3.13.13 六组完整矩阵来自 `b35dde2`，此后 Python 实现未变。没有伪称新增远程 CI 运行。

## 历史与剩余事项

保留旧 `4a5972e` 的三批结果；合约批次为 15/18、含一次误交付。中间 `f2d23ca` 完整批次为 12/18，未观察误交付；其三条修改首次仅一条成功。过严的 `d4e16b0` 中止试验、单底图和密集布局诊断均单独保存，不并入正式首次矩阵。测试措辞的修正和拒收证据均保留。

全部实拍、输出、提示词和宿主事件在仓库外私密保存。三场景合成示例已选定第一轮输出作为待审材料，未公开。剩余发布障碍是上述生成质量、人工审阅和许可/公开决定；没有推送、关闭 issue、发送评论或发布。
