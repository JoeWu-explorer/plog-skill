# 人物保真与交付核验修复

当前保留包为 `photo-dialogue-2a6156c.zip`，运行提交 `2a6156c5369a30c8ad28ec1e79533c4aa286671c`，SHA-256 为 `719007587cc894b324ea3f7c2407c0e9a95a7cd2b39db29c3fe4f7eeed2ca9bd`。新的完整矩阵未达到发布门槛；不能把 #26 或父任务 #17 标为验收完成。

## 最终保留包的独立结果

同九张固定输入各两轮新会话，18 次真实内置图像调用全部执行。11 次通过独立底线检查，7 次被拒收；所有接受的成图均检查完整图和 390 CSS px 对照图，未观察到人物误交付。人工五项评分全部留空。18 个源哈希、安装清单和原生调用事件已核对；11 个保存的 PNG 通过记录、格式与元数据检查，预览和下载均为实际存在的绝对路径。

- 合成 01–05 两轮通过，四人图文字偏小但仍可辨，艺术评分待人工。
- 合成 06 第一轮通过，第二轮宿主因人物位置及间距变化拒收。
- 三张实拍均两轮拒收。宿主报告近景文字偏小或补出眼口细节、双人鼻唇结构变化，以及低照多人眼耳结构变化。这些拒收理由保留为宿主判断，未将未保存候选声称为独立确认的身份变化。

该结果低于至少 16/18、每张输入至少一次成功的门槛。人工评分不能抵消七次拒收。拒收保护在这些实例生效，也不能证明生成保真能力已修复。

## 修改、行为和工程检查

最终包三项自然语言修改各一次真实调用，全部通过完整图与手机宽度核验：仅改文案、减轻氛围、从指定旧版移字。测试从历史已接受的 v001 作品副本恢复，均正确指向 v001，未混入其他版本文案，已有 PNG 哈希保持。文字、无障碍与明确标注“重建、非历史原文”的提示词导出已核对。这不是精确像素编辑测试，历史严格 80px 位移失败仍保留。

15 类行为全部执行，14 类通过，一类保留首次交付失败。17 次受控服务调用核对事件与作品状态，首次/修改失败不替换旧作品，明确重试仅追加一次调用，停止后迟到候选不保存；另有七个零生成边界会话与三个实际生成的补充行为会话。替身通过不代表真实人物识别准确。

长繁体原话的补充测试保留完整文字、原 1024×1535 尺寸，完整图和手机宽度可读，但最终回复用了相对 PNG 链接。该缺陷另行记录，不并入正式 18 次成绩，也不因事后重显链接而把首次失败改成通过。已在新的只读会话正确重显该作品的绝对路径预览和下载，零图像调用、文件哈希不变；这只修复该作品的入口，绝对路径指令仍不能保证宿主每次遵守。

可复现构建、独立安装/自检/卸载、34 项本机测试、类型与仓库检查通过。macOS/Linux arm64 × CPython 3.11.15、3.12.13、3.13.13 六组矩阵来自 `b35dde2`，此后 Python 实现未变；没有伪称新的远程 CI 运行。

## 已撤回试验与历史

新增 `142b30e` 提示要求目标字高与低照不增强，完整 18 次仅 9 次接受、9 次拒收。三种修改通过，但更严格措辞没有解决小字、重构面部或裁构图；已撤回其运行变化，最终包恢复为 `2a6156c` 的逐字节相同内容。新增回归用例保留。该候选的部分故障试验不计完整行为通过。详见 [生成约束诊断](generation-limit-diagnosis.md)。

各批次失败原样保留，不跨批选优：`4a5972e` 合约批次 15/18，含一次人物误交付；`f2d23ca` 为 12/18、未观察误交付；`ad0104e` 为 15/18、未观察误交付，但两项相对链接失败。`ad0104e` 三种修改首次 2/3，额外自然语言旧版移字成功。其五个只读诊断、十五类行为及两项随后链接回归均属于历史证据，不能覆盖本次失败。过严的 `d4e16b0` 中止试验和独立布局诊断也保留。

## 当前处理边界

代码与交付工程检查完成，纯生成质量仍未达到原规格。保留完整失败记录与私密对照页，不继续重复批次来凑通过。改用受控文字层会改变已确认“整体生成、禁止本地贴字”的产品约束，尚未获准，未实现；降低发布范围也未自行执行。

实拍、成图、提示词和事件全部在仓库外私密保存。人工审阅、历史图许可与最终公开决定尚未完成；没有推送、关闭 issue、发送评论或发布。

最终文档按 Standards 与 Spec 两条轴复查，均发现同一处历史 JSON 统计串批；已恢复历史提交对应的原始数据，两轴定向复核均无剩余项。此结论只针对本次文档变更，不能解释为未达到的产品验收已通过。
