# 发布检查

当前预发布为 **v0.1.0-alpha.7**，固定包、源提交与验证范围见 [版本清单](release.json)。本次已获所有者发版授权，沿用现有公开仓库。

## 本次预发布

- [x] 故事书功能在最新 main 上集成；固定运行提交重复构建的 ZIP 完全一致。
- [x] 69 项确定性测试、类型检查、37 个运行引用与 70 项资产清单的公开许可检查通过。
- [x] 全新安装／自检／独立成册／卸载通过；真实 alpha.6 发布包经 SHA256 核验，升级、自检、成册和卸载通过，作品保留。
- [x] 安装包生成的 HTML 通过桌面、手机、正反翻页、键盘、自动翻阅、MP4 导出、取消、保存后重开、打印、减少动态效果和无脚本阅读检查；无脚本错误和外部网络请求。
- [x] 实际导出 MP4 为 H.264、1280×960、约 20 秒，解码检查通过。
- [x] 附件仅为允许名单运行 ZIP 与 SHA256SUMS，不包含照片、视频、私密图册稿或本地路径。
- [x] 发布说明明确 Canvas 模拟翻页、浏览器录制支持范围及新照片端到端验收尚未完成；没有将样册验证冒充完整视觉矩阵。

托管矩阵与合并检查以 [本次 PR checks](https://github.com/JoeWu-explorer/plog-skill/pull/42/checks) 的实际结果为准，本地检查记录独立保存。alpha.6 的历史发布与 CI 状态见 [历史清单](history/release-alpha-6.json)。

## 后续完整验收

- [ ] 各目标 Agent／provider 组合完成首次尝试、行为与修改矩阵，真人评分达标，详见 [验收标准](acceptance.md)。
- [ ] 一组新用户照片从逐张生成到故事书和动画的完整端到端验收。
- [ ] 正式示例完成人工视觉审阅。

仓库及既有素材的公开范围沿用所有者先前授权和 [公开检查记录](public-repository-setup.md)。预发布不表示以上未完成项已通过。
