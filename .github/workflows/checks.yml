name: Deterministic checks
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  files-and-package:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python: ['3.11', '3.12', '3.13']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m pip install --require-hashes -r plog/requirements.lock -r tests/requirements-dev.lock
      - run: python plog/scripts/self_check.py --workspace "${{ runner.temp }}"
      - run: python -m unittest discover -s tests
      - run: python -m mypy --ignore-missing-imports plog/scripts scripts
      - run: python scripts/check_repository.py
      - run: python scripts/distribution.py build "${{ runner.temp }}/plog.zip"
      - run: python scripts/distribution.py install "${{ runner.temp }}/plog.zip" --target "${{ runner.temp }}/skills/plog"
      - run: python "${{ runner.temp }}/skills/plog/scripts/self_check.py" --workspace "${{ runner.temp }}"
      - run: python -m pip install --require-hashes -r plog/requirements-heif.lock
      - run: python plog/scripts/self_check.py --workspace "${{ runner.temp }}"
      - run: python scripts/distribution.py uninstall --target "${{ runner.temp }}/skills/plog"
